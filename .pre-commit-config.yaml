repos:
  - repo: local
    hooks:
      - id: ruff-system
        name: ruff (system)
        entry: py -m ruff          # 👈 use the Python launcher
        language: system
        types: [python]
        args: ["check", "--fix"]
