[tool.ruff]
line-length = 88
target-version = "py312"
extend-ignore = ["E203", "W503"]
exclude = [".git", "__pycache__", "build", "dist", "venv", ".venv", "node_modules"]

[tool.ruff.per-file-ignores]
# allow tests to use print or slow assertions
"tests/*" = ["T001"]
